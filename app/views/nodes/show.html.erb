<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strongt >
  <%= @node.description %>
</p>

Options:
<ul>
  <% @node.exits().each do |path| %>
    <li>
      Exit = <%= link_to( path.start_description, path.end_node ) %>
    </li>
  <% end %>
</ul>

<!-- <%= link_to 'Edit', edit_node_path(@node) %> |
  <%= link_to 'Back', nodes_path %> -->
